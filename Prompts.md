TOOL : CHATGPT
PROMPT:Create a minimal FastAPI backend for an AI Interview Agent.
       Requirements:
       FastAPI app 
       GET `/health` endpoint returning `{ "status": "ok" }` 
       POST `/api/interview` endpoint returning: 
       {
         "reply": "Interview endpoint working",
         "done": false
       }
       Keep the code modular and ready for future interview logic.
